# Portico

The command surface for .NET backend services — contract-first CLI routing where
your examples are tests, verified at compile time.

> This is a placeholder. The real README lands with POR-7, once the framework builds.
